DATABASE_URL="postgresql://app_financiera:app_financiera@localhost:5432/app_financiera?schema=public"
JWT_SECRET="cambia-esto-por-un-secreto-largo-y-aleatorio"
JWT_EXPIRES_IN="7d"
PORT=3000
INVITE_EXPIRATION_DAYS=7
# Opcional: restringe el origen permitido por CORS (ej: http://localhost:5173).
# Si se omite, se permite cualquier origen (valido para desarrollo local).
CORS_ORIGIN=
